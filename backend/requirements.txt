# SC Ore Scanner - Backend Dependencies (full install)
#
# `pip install -r requirements.txt` installs EVERYTHING needed to run the app,
# exactly as before. Dependencies are organized into two files:
#   - requirements-core.txt : FastAPI, capture, image processing, config
#   - requirements-ml.txt   : OCR/ML stack (easyocr, torch, torchvision)
#
# See requirements-dev.txt for the test/lint tooling.

-r requirements-core.txt
-r requirements-ml.txt
