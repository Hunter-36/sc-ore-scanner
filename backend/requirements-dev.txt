# SC Ore Scanner - Development & test dependencies
# Installs the core runtime (no ML) plus the test/lint toolchain.
# Unit tests and the resolver/config/server suites run against this alone.
# The OCR end-to-end suite additionally needs requirements-ml.txt.

-r requirements-core.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # FastAPI TestClient
ruff>=0.5.0
