{
  "identifier": "default",
  "description": "Permissions for the overlay + calibration windows: close, drag-to-move.",
  "windows": ["main", "calibrate"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
